# Homebrew Tap for Par Term

This tap hosts the official `par-term` cask.

```bash
brew tap paulrobello/par-term
brew install --cask par-term
```

The cask definition is synced from the main [paulrobello/par-term](https://github.com/paulrobello/par-term) repository.
